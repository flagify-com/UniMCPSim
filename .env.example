# =============================================================================
# UniMCPSim 环境配置示例
# =============================================================================
# 使用方法：将此文件复制为 .env 并根据需要修改配置
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# 服务端口配置
# -----------------------------------------------------------------------------

# MCP 服务器端口（默认：9090）
MCP_SERVER_PORT=9090

# 管理后台端口（默认：9091）
ADMIN_SERVER_PORT=9091

# -----------------------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------------------

# 调试模式（true/false，默认：false）
# 启用后会记录完整的请求/响应内容，仅用于排查问题
DEBUG=false

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# 日志目录（默认：logs）
LOG_DIR=logs

# -----------------------------------------------------------------------------
# 大模型配置（可选 - 推荐使用 Web 界面配置）
# -----------------------------------------------------------------------------
#
# 🌟 推荐方式：通过 Web 管理界面配置
#    1. 启动服务：python start_servers.py
#    2. 访问：http://localhost:9091/admin/llm-config
#    3. 在界面中配置 API Key、模型等参数
#    4. 点击"测试连接"验证后保存
#
# 配置优先级：数据库配置（Web界面）> 环境变量（本文件）
#
# 以下环境变量仅作为兜底方案，适用于：
#   - 首次部署时快速配置
#   - CI/CD 自动化部署
#   - Docker 环境变量注入
#
# 如已通过 Web 界面配置，以下内容可忽略
# -----------------------------------------------------------------------------

# API Key（支持 OpenAI、阿里云通义、智谱等兼容 OpenAI API 的服务）
# OPENAI_API_KEY=sk-xxx

# API 地址
# - OpenAI: https://api.openai.com/v1
# - 阿里云通义: https://dashscope.aliyuncs.com/compatible-mode/v1
# - 智谱 AI: https://open.bigmodel.cn/api/paas/v4
# OPENAI_API_BASE_URL=https://api.openai.com/v1

# 模型名称
# - OpenAI: gpt-4o-mini, gpt-4o, gpt-4
# - 阿里云: qwen-max, qwen-plus, qwq-32b
# - 智谱: glm-4, glm-4-flash
# OPENAI_MODEL=gpt-4o-mini

# 思考模式（默认：false）
# 某些模型支持输出思考过程，但会影响 JSON 解析，建议保持 false
# OPENAI_ENABLE_THINKING=false

# 流式模式（默认：false）
# 某些模型（如 qwq-32b）强制要求启用
# OPENAI_STREAM=false

# =============================================================================
